# GPT-Engineer 核心依赖
openai>=1.0
termcolor==2.3.0
typer>=0.3.2
rudder-sdk-python>=2.0.2
dataclasses-json==0.5.7
tiktoken>=0.0.4
tabulate==0.9.0
python-dotenv>=0.21.0
langchain>=0.1.2
langchain_openai
toml>=0.10.2
tomlkit>=0.12.4
pyperclip>=1.8.2
langchain-anthropic>=0.1.1
regex>=2023.12.25
pillow>=10.2.0
datasets>=2.17.1
black==23.3.0
langchain-community>=0.2.0
backoff>=2.0.0

# Web框架和API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
jinja2>=3.0.0

# HTTP客户端
requests>=2.28.0
aiohttp>=3.8.0

# 数据库
sqlalchemy>=2.0.0
sqlite3  # 内置模块，但列出来以示明确

# 异步支持
asyncio  # 内置模块
aiofiles>=22.0.0

# 开发和测试
pytest>=7.3.1
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# 其他工具
pathlib  # 内置模块
datetime  # 内置模块
uuid  # 内置模块
json  # 内置模块
hashlib  # 内置模块
pickle  # 内置模块
zipfile  # 内置模块
tarfile  # 内置模块
tempfile  # 内置模块
subprocess  # 内置模块
shutil  # 内置模块
threading  # 内置模块
time  # 内置模块