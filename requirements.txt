# AI Development Platform Dependencies
# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==11

# Database


# AI and ML
openai==1.26.0
gpt-engineer==0.3.1

# Blockchain
solana>=0.32
base58==2.1.1

# Data Processing
pyyaml==6.0.1

# HTTP and API
requests==2.31.0
httpx==0.27

# Utilities
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# Development and Testing
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.3.0
flake8==6.1.0

# Optional: For enhanced features
redis==5.0.1
celery==5.3.4
prometheus-client==0.19.0

aiosignal==1.3.2
aiohttp==3.12.12

langchain-core==0.2.26          # 核心依赖，0.2.x 系列完全支持 Python 3.12
langchain==0.2.7                # 主库，0.2.7+ 修复了 Python 3.12 相关兼容问题
langchain-community==0.2.7      # 社区集成库，与主库版本保持一致
langsmith==0.1.83               # 调试工具，0.1.80+ 支持 Python 3.12

# 模型集成库（适配 Python 3.12 且与核心库兼容）
langchain-openai==0.1.9         # OpenAI 集成，0.1.8+ 支持 Python 3.12
langchain-anthropic==0.1.23     # Anthropic 集成，0.1.10+ 修复 3.12 兼容性


# 依赖库（需显式指定以避免版本冲突）
anthropic==0.48.0               # Anthropic 官方 SDK，0.19.0+ 支持 Python 3.12
#openai==1.30.1                  # OpenAI 官方 SDK，1.28.0+ 完善 3.12 支持
python-dotenv==1.0.0            # 环境变量管理，完全兼容 3.12
pydantic==2.7.1                 # 数据验证，2.5.0+ 对 3.12 支持更稳定

langchain-aws==0.1.7            # AWS 服务集成（S3、Bedrock等），0.1.5+ 支持 Python 3.12
langchain-cohere==0.1.8         # Cohere 模型集成，0.1.6+ 适配 3.12
langchain-mistralai==0.1.7      # Mistral AI 模型集成，0.1.5+ 支持 3.12
langchain-tools==0.1.20         # 通用工具集（搜索、计算器等），0.1.18+ 兼容 3.12

# ------------------------------
# 2. 文档处理（加载、解析、分割）
# ------------------------------
langchain-text-splitters==0.2.1 # 文本分割工具，独立包更稳定，0.2.x 支持 3.12
pypdf==4.2.0                    # PDF 文档加载，4.0.0+ 完全兼容 Python 3.12
python-docx==1.1.2              # Word 文档加载，1.1.0+ 支持 3.12
python-pptx==0.6.23             # PPT 文档加载，0.6.21+ 适配 3.12
unstructured==0.14.2            # 非结构化文档解析（HTML/Markdown等），0.12.x 支持 3.12
markdown-it-py==3.0.0           # Markdown 解析依赖，3.0.0+ 兼容 3.12

# ------------------------------
# 3. 向量存储与检索
# ------------------------------
chromadb==0.5.0                 # 本地向量数据库，0.5.0+ 修复 3.12 兼容性问题
pinecone-client==3.2.2          # Pinecone 向量数据库客户端，3.2.0+ 支持 3.12
weaviate-client==4.5.1          # Weaviate 向量数据库客户端，4.4.0+ 适配 3.12
faiss-cpu==1.8.0                # 向量检索库（CPU版），1.7.4+ 支持 3.12（需手动安装依赖）


tenacity==8.2.3                 # 重试机制，LangChain 核心依赖，8.2.0+ 兼容 3.12
PyYAML==6.0.1                   # YAML解析，配置文件处理，6.0.1+ 适配 3.12



# 模型 SDK（与 langchain-anthropic 兼容）  # 满足 >=0.28.0 且兼容 jiter
jiter==0.9.1       # 满足 <1,>=0.4.0，适配 Python 3.12

# Celery 依赖（精确版本，避免 kombu 冲突）
kombu==5.5.3       # 满足 <6.0,>=5.3.2，兼容 celery 5.3.4
amqp==5.3.0        # 兼容 kombu 5.5.3
tzdata==2025.2     # 满足 celery 对时区数据的要求

# 其他依赖（避免隐式冲突）
idna==3.9          # 兼容 requests 2.31.0
h11==0.13.0        # 兼容 uvicorn 0.24.0





# GPT-Engineer 核心依赖
termcolor==2.3.0
typer>=0.3.2
rudder-sdk-python>=2.0.2
dataclasses-json==0.5.7
tiktoken>=0.0.4
tabulate==0.9.0
python-dotenv>=0.21.0
langchain>=0.1.2
langchain_openai
toml>=0.10.2
tomlkit>=0.12.4
pyperclip>=1.8.2
langchain-anthropic>=0.1.1
regex>=2023.12.25
pillow>=10.2.0
datasets>=2.17.1
langchain-community>=0.2.0
backoff>=2.0.0

# Web框架和API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
jinja2>=3.0.0

# HTTP客户端
requests>=2.28.0
aiohttp>=3.8.0

# 数据库
sqlalchemy>=2.0.0

aiofiles>=22.0.0


Crypto

